# Django API base URL. Change this after deploying the backend.
VITE_API_URL=http://127.0.0.1:8000/api

# When true, network failure automatically falls back to local demo data.
VITE_DEMO_FALLBACK=true
